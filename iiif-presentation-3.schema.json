{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IIIF Presentation 3.0 Manifest (minimal)",
  "description": "A minimal, growable schema for an IIIF Presentation API 3.0 Manifest. ponytail: validates the Manifest and one level down (Canvas); does NOT descend into AnnotationPage/Annotation/body, nor check metadata/thumbnail/provider. Add those levels when a real manifest needs them.",
  "type": "object",
  "required": ["@context", "id", "type", "label", "items"],
  "properties": {
    "@context": {
      "description": "Must reference the IIIF Presentation 3 context; may be a single string or an array of strings.",
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ]
    },
    "id": {
      "description": "The URI that identifies this Manifest.",
      "type": "string"
    },
    "type": {
      "description": "Must be the literal string 'Manifest'.",
      "const": "Manifest"
    },
    "label": { "$ref": "#/definitions/languageMap" },
    "items": {
      "description": "The list of Canvases that make up the Manifest.",
      "type": "array",
      "items": { "$ref": "#/definitions/canvas" }
    }
  },
  "definitions": {
    "languageMap": {
      "description": "A language map: keys are language codes (or 'none'), values are arrays of strings.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },
    "canvas": {
      "description": "A Canvas: needs id and type, and either width+height (spatial) or duration (temporal). width and height must appear together.",
      "type": "object",
      "required": ["id", "type"],
      "properties": {
        "id": { "type": "string" },
        "type": { "const": "Canvas" },
        "label": { "$ref": "#/definitions/languageMap" },
        "height": { "type": "integer", "minimum": 1 },
        "width": { "type": "integer", "minimum": 1 },
        "duration": { "type": "number", "exclusiveMinimum": 0 },
        "items": { "type": "array" }
      },
      "dependencies": {
        "height": ["width"],
        "width": ["height"]
      },
      "anyOf": [
        { "required": ["height", "width"] },
        { "required": ["duration"] }
      ]
    }
  }
}
